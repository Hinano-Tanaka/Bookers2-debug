<% if book.favorited_by?(current_user) %>
  <p>
    <%= button_to book_favorites_path(book), remote: true, method: :delete do %>
      <i class="fas fa-heart" aria-hidden="true"></i>
      <%= book.favorites.count %>
    <% end %>
  </p>
  <% else %>
  <p>
    <%= button_to book_favorites_path(book), remote: true, method: :post do %>
      <i class="fas fa-heart" aria-hidden="true"></i>
      <%= book.favorites.count %>
    <% end %>
  </p>
<% end %>
